# ETL-pipeline
I built a production-ready ETL pipeline in Python that validates, preprocesses, engineers features, and transforms structured data using Pandas and scikit-learn, with logging and automated reporting.
